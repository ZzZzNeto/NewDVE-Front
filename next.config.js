/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['127.0.0.1','patasdacasa.com.br','','api.geoapify.com','maps.geoapify.com'],
      },
}

module.exports = nextConfig
